services:
  fastify:
    build: ./Backend
    container_name: fastify
    ports:
      - "8443:8443"
    command: start
    volumes:
      - ./Backend:/app
      - sqlite:/app/data

  typescript:
    build: ./Frontend
    container_name: typescript
    ports:
      - "8080:8080"
    volumes:
      - ./Frontend:/app
      - /app/node_modules


volumes:
  sqlite:
